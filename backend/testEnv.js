// Load environment variables from .env file
require('dotenv').config();

// Log the Mongo URI to confirm it's being loaded
// console.log('Mongo URI:', process.env.MONGO_URI);
// console.log('All Environment Variables:', process.env);